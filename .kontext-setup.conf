# Kontext setup defaults
# Copy to ~/.kontext-setup.conf and edit as needed.

INSTALL_JAVA=true
INSTALL_DOCKER=true
INSTALL_MISC=true

# Optional: override APP_CONFIG_PATH for deployments
# APP_CONFIG_PATH=/app/config/application.conf
